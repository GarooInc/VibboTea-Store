{% doc %}
  Renders a compact row of product features with icons.
  
  Displays 4 key product characteristics: 100% Natural, No Added Sugar,
  No Microplastics, and 30-Day Guarantee. Layout is responsive with
  2x2 grid on mobile/tablet and a single horizontal row on large desktop.

  @example
  {% render 'product-features-row' %}
{% enddoc %}

<div class="product-features-row">
  <div class="product-features-row__grid">
    
    <!-- 100% Natural -->
    <div class="product-features-row__item">
      <div class="product-features-row__icon-wrap">
        <img 
          src="{{ '100-natural.svg' | asset_url }}" 
          alt="100% Natural" 
          class="product-features-row__icon"
          width="96"
          height="96"
        >
      </div>
      <span class="product-features-row__text">{{ 'product_features.natural' | t }}</span>
    </div>

    <!-- No Added Sugar -->
    <div class="product-features-row__item">
      <div class="product-features-row__icon-wrap">
        <img 
          src="{{ 'no-added-sugar.svg' | asset_url }}" 
          alt="No Added Sugar" 
          class="product-features-row__icon"
          width="96"
          height="96"
        >
      </div>
      <span class="product-features-row__text">{{ 'product_features.no_sugar' | t }}</span>
    </div>

    <!-- No Microplastics -->
    <div class="product-features-row__item">
      <div class="product-features-row__icon-wrap">
        <img
          src="{{ 'no-microplastics.svg' | asset_url }}"
          alt="No Microplastics"
          class="product-features-row__icon product-features-row__icon--microplastics"
          width="96"
          height="96"
        >
      </div>
      <span class="product-features-row__text">{{ 'product_features.no_microplastics' | t }}</span>
    </div>

    <!-- 30-Day Guarantee -->
    <div class="product-features-row__item">
      <div class="product-features-row__icon-wrap">
        <img
          src="{{ '30-day-guarantee.svg' | asset_url }}"
          alt="30-Day Guarantee"
          class="product-features-row__icon product-features-row__icon--guarantee"
          width="96"
          height="96"
        >
      </div>
      <span class="product-features-row__text">{{ 'product_features.guarantee' | t }}</span>
    </div>

  </div>
</div>

{% stylesheet %}
  .product-features-row {
    width: 100%;
    max-width: 100%;
    min-width: 0;
    padding: 0.5rem 0;
    margin: 0;
    background-color: #ffffff;
    overflow: hidden;
  }

  .product-features-row__grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.35rem 1rem;
    width: 100%;
    max-width: 100%;
    min-width: 0;
  }

  @media (min-width: 768px) {
    .product-features-row {
      padding: 0;
      margin: 0;
    }
  }

  .product-features-row__item {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    align-items: center;
    column-gap: 0.25rem;
    width: 100%;
    min-width: 0;
  }

  .product-features-row__icon-wrap {
    width: fit-content;
    display: flex;
    align-items: center;
    justify-content: flex-start;
    flex-shrink: 0;
  }

  .product-features-row__icon {
    width: 36px;
    height: 36px;
    flex-shrink: 0;
    display: block;
    object-fit: contain;
    object-position: left center;
  }

  /* These two icons have asymmetric artwork (bottle cap, ribbon tails) that
     sits off-center within their tight crop, so their visual "core" shape
     needs a nudge to look level with the other icons. */
  .product-features-row__icon--microplastics {
    transform: translateY(-10%);
  }

  .product-features-row__icon--guarantee {
    transform: translateY(14%);
  }

  .product-features-row__text {
    font-family: var(--font-bricolage, sans-serif);
    font-size: 0.6rem;
    font-weight: 700;
    line-height: 1.15;
    color: #1a1a1a;
    text-transform: uppercase;
    letter-spacing: 0.005em;
    text-align: left;
    flex: 1;
    min-width: 0;
    align-self: center;
    overflow-wrap: normal;
    word-break: normal;
  }

  @media (min-width: 768px) {
    .product-features-row__text {
      font-size: 0.65rem;
    }
  }

  @media (min-width: 1024px) {
    .product-features-row__grid {
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 0.4rem 0.5rem;
      align-items: stretch;
    }

    .product-features-row__item {
      column-gap: 0.2rem;
    }

    .product-features-row__icon-wrap {
      width: fit-content;
    }

    .product-features-row__icon {
      width: 27px;
      height: 27px;
    }

    .product-features-row__text {
      text-align: left;
      font-size: 0.55rem;
      line-height: 1.2;
    }
  }

  @media (min-width: 1280px) {
    .product-features-row__grid {
      gap: 0.5rem 0.65rem;
    }

    .product-features-row__item {
      column-gap: 0.2rem;
    }

    .product-features-row__icon {
      width: 33px;
      height: 33px;
    }

    .product-features-row__text {
      font-size: 0.6rem;
    }
  }

  @media (min-width: 1440px) {
    .product-features-row__icon {
      width: 36px;
      height: 36px;
    }

    .product-features-row__text {
      font-size: 0.65rem;
    }
  }
{% endstylesheet %}
