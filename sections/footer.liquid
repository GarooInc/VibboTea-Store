<footer 
  class="w-full h-[55vh]  bg-gray-950 text-white flex items-start justify-between"
  style="width: 100vw; margin-left: calc(50% - 50vw);"
>
  <!-- COLUMNA IZQUIERDA -->
  <div class="w-1/2 flex flex-col justify-between items-start  pl-20 pt-20 h-full">
    <div>
    {% if section.settings.logo %}
      <img 
        src="{{ section.settings.logo | image_url: width: 300 }}" 
        alt="Logo"
        class="w-40 object-contain "
      >
    {% endif %}
  <!-- descripcion debajo del footer -->
    {% if section.settings.descripcion %}
      <p class="text-[1vw] text-white mt-2 pt-5 pr-[15vw] leading-tight">
        {{ section.settings.descripcion }}
      </p>
    {% endif %}</div>

      <!-- seccion de iconos de redes sociales -->
      <div class="flex pl-2 gap-4 pb-20">
        <div class="flex justify-end md:justify-between ">
            <a href="{{ section.settings.facebook_url }}" target="_blank" rel="noopener" 
              class="footer-icon hover:opacity-70 transition-opacity scale-80" 
              aria-label="Facebook">
              {%- render 'fb' -%}
            </a>
        </div>
        <div class="flex justify-end md:justify-between ">
            <a href="{{ section.settings.instagram_url }}" target="_blank" rel="noopener" 
              class="footer-icon hover:opacity-70 transition-opacity scale-80" 
              aria-label="Instagram">
              {%- render 'ig' -%}
            </a>
        </div>
        <div class="flex justify-end md:justify-between ">
            <a href="{{ section.settings.tiktok_url }}" target="_blank" rel="noopener" 
              class="footer-icon hover:opacity-70 transition-opacity scale-80" 
              aria-label="Tiktok">
              {%- render 'tiktok' -%}
            </a>
        </div>
       
      </div>

      <div class="flex flex-row space-y-1 md:pb-15">
        <a class="hover:opacity-70 transition-opacity pr-5">Privacy Policy</a>
        <a class="hover:opacity-70 pr-5">Terms of Service</a>
        <a class="hover:opacity-70 ">Cookies Policy</a>
      </div>

      </div>

  <!-- COLUMNA DERECHA -->
  <div class="w-1/2 h-full flex flex-col items-start justify-start px-20 pt-15 pr-20 pl-[10vw]">
      <nav class="flex flex-row justify-between w-full">
        
        {% for link in section.settings.menu.links %}
          <div class="flex flex-col items-start  pt-8">
          <a href="{{ link.url }}" class=" text-[1.8vw] hover:text-gray-300 transition whitespace-nowrap ">
            {{ link.title }}
          </a>   
          <p class="text-xs">Vision & mission</p> 
        </div> 
        {% endfor %}
      </nav>
      
  </div>

</footer>




{% schema %}
{
  "name": "t:general.footer",
  "settings": [
    {
      "type": "link_list",
      "id": "menu",
      "label": "t:labels.menu"
    },
   
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo Image"
    },
    {
      "type": "text",
      "id": "descripcion",
      "label": "Description",
      "default": "Bienvenido a Nuestra Tienda"
    },
    {
      "type": "checkbox",
      "id": "show_payment_icons",
      "label": "t:labels.show_payment_icons",
      "default": true
    },
    {
      "type": "url",
      "id": "facebook_url",
      "label": "Facebook URL"
    },
    {
      "type": "url",
      "id": "instagram_url",
      "label": "Instagram URL"
    },
    {
      "type": "url",
      "id": "tiktok_url",
      "label": "Tiktok URL"
    },
  ]
}
{% endschema %}
